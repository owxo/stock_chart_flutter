# 2026-06-20 开发日志

## 已完成

- 建立 `docs` 项目开发文档目录，补充需求、技术、设计和执行步骤标准文件。
- 建立 `docs/development-log` 每日开发日志目录，新增日志说明、模板和当天日志。
- 新增根目录 `APP.md`，作为后续协作入口，明确标准文件路径和每日开发日志更新规则。
- 完成一次项目优化审查，梳理生成文件跟踪、验证命令、示例页拆分、图表性能和测试覆盖等优化方向。
- 新增根目录 `.gitignore`，将 Flutter/Dart 生成文件、构建产物和 IDE 本地文件纳入忽略范围。
- 将已跟踪的 `.dart_tool` 生成文件移出 Git 跟踪，避免旧机器路径影响格式化、分析和测试。
- 同步包版本到 `0.1.1`，更新 README 安装示例、发布前验证步骤和 CHANGELOG。
- 为 `CandleData`、`LinePoint` 增加值相等、`hashCode` 和 `toString`，并新增根目录模型测试。
- 将腾讯行情请求和 JSON/JSONP 解析从 `TencentStockChartPage` 抽离到 `TencentQuoteDataSource`，并增加请求超时控制。
- 为腾讯行情数据源新增单元测试，覆盖分时 JSONP、五日分时、K 线解析和非 200 响应错误。
- 将 `TencentStockChartPage` 内的自定义绘制器拆分到 `tencent_stock_chart_painters.dart`。
- 将 `TencentStockChartPage` 内的右侧盘口和成交面板拆分到 `tencent_stock_chart_right_panel.dart`。
- 优化 K 线图 MA 绘制逻辑，使用收盘价前缀和避免每个点重复窗口求和。
- 补充图表边界 Widget 测试，覆盖空数据、单点数据、同价数据和空 MA 颜色列表。
- 为折线图增加数据长度变化时清理选中索引的逻辑，避免数据更新后保留过期选中状态。
- 修复五日图相关问题：五日接口日期支持 `YYYY-MM-DD` 清洗解析、五日成交量柱宽按 multiDay 总 slot 计算、分时坐标按真实交易时段映射。
- 示例 Widget 测试增加点选五日页签后的渲染检查。
- 在 `pubspec.yaml` 增加 `platforms` 声明，明确支持 Android、iOS、Web、macOS、Windows 和 Linux。
- 在 README 增加支持平台说明，明确本包为纯 Flutter widget/painter 实现。
- 新增 `.pubignore`，避免内部协作文档和本地 IDE 元数据进入发布包。
- 将已跟踪的 `.idea` 本地 IDE 文件移出 Git 跟踪，修复发布校验中“已跟踪但被忽略”的提示。
- 构建并运行 example Web 版本，生成页面截图用于视觉检查。
- 清除腾讯行情示例页默认数据：组件默认 `symbol` 为空，example 不再默认传入 `sz001896`，仅在调用方显式传入股票代码时请求远程行情。
- 移除腾讯行情示例页本地模拟行情兜底，网络失败时显示错误状态但不填充默认行情。
- 为 example 增加股票代码输入栏和加载按钮，支持输入 `sz001896`、`sh600519` 或 6 位 A 股代码并自动规范化。
- 为 example 增加 URL 查询参数初始化支持，`?symbol=sz001896` 会自动填入并传给 `TencentStockChartPage` 加载行情。
- 修复五日分时图网格分区：多日模式按交易日数量绘制竖向边界，五日图至少显示 5 个日框，成交量区域同步同样分区。
- 将插件版本升级到 `0.1.2`，同步 README 安装示例、CHANGELOG 和 GitHub 仓库元数据。
- 将 `output/` 加入 `.gitignore` 和 `.pubignore`，避免本地截图产物进入 GitHub 提交或 pub 发布包。
- 正式发布 `stock_chart_flutter 0.1.2` 到 pub.dev。

## 变更文件

- `APP.md`
- `docs/README.md`
- `docs/requirements.md`
- `docs/technical-spec.md`
- `docs/design-spec.md`
- `docs/execution-steps.md`
- `docs/development-log/README.md`
- `docs/development-log/TEMPLATE.md`
- `docs/development-log/2026-06-20.md`
- `.gitignore`
- `.pubignore`
- `README.md`
- `CHANGELOG.md`
- `pubspec.yaml`
- `pubspec.lock`
- `example/lib/main.dart`
- `example/pubspec.lock`
- `lib/src/models/candle_data.dart`
- `lib/src/models/line_point.dart`
- `lib/src/tencent_quote_data_source.dart`
- `lib/src/tencent_stock_chart_painters.dart`
- `lib/src/tencent_stock_chart_page.dart`
- `lib/src/tencent_stock_chart_right_panel.dart`
- `lib/src/stock_candlestick_chart.dart`
- `lib/src/stock_line_chart.dart`
- `test/chart_edge_cases_test.dart`
- `test/models_test.dart`
- `test/tencent_quote_data_source_test.dart`
- `example/test/widget_test.dart`
- `.dart_tool/package_config.json`
- `.dart_tool/package_graph.json`
- `.dart_tool/version`
- `.idea/.gitignore`
- `.idea/libraries/Dart_Packages.xml`
- `.idea/libraries/Dart_SDK.xml`
- `.idea/libraries/Flutter_Plugins.xml`
- `.idea/modules.xml`
- `.idea/stock_chart_flutter.iml`
- `output/playwright/stock-chart-web.png`

## 验证

- 初始文档搭建阶段仅新增 Markdown 文档和协作规范，未修改 Dart 业务代码，未运行 Flutter 分析或测试命令。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：失败，原因是根目录不存在 `test/` 目录。
- 运行根目录指定 `example/test/widget_test.dart`：失败，原因是从根目录执行时无法解析 `stock_chart_flutter_example` 示例包。
- 在 `example/` 目录运行 `flutter test`：通过，示例 Widget 测试全部通过。
- 运行 `dart format lib test`：通过，未产生格式化变更。
- 再次运行 `flutter analyze`：通过，未发现分析问题。
- 再次运行根目录 `flutter test`：通过，新增模型测试全部通过。
- 再次在 `example/` 目录运行 `flutter test`：通过，示例 Widget 测试全部通过。
- 运行 `dart format lib test`：通过，格式化新增数据源测试文件。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，模型和腾讯行情数据源测试全部通过。
- 在 `example/` 目录运行 `flutter test`：通过，示例 Widget 测试全部通过。
- 运行 `dart format lib test`：通过，拆分后的 part 文件和新增边界测试格式正常。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，图表边界、模型和数据源测试全部通过。
- 在 `example/` 目录运行 `flutter test`：通过，示例 Widget 测试全部通过。
- 运行 `dart format lib test example/test`：通过，五日相关修复格式正常。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，五日数据源日期解析和图表边界测试通过。
- 在 `example/` 目录运行 `flutter test`：通过，点选五日页签后无异常。
- 运行 `dart format lib test example/test`：通过，平台声明和发布包整理未引入格式化问题。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，全部根目录测试通过。
- 在 `example/` 目录运行 `flutter test`：通过，示例 Widget 测试通过。
- 运行 `flutter pub publish --dry-run`：第一次发现内部 `docs/`、`APP.md`、`.idea` 进入发布校验范围；新增 `.pubignore` 并移出 `.idea` 跟踪后再次运行，仅剩工作区未提交提醒。
- 运行 `flutter build web`：通过，生成 `example/build/web`。
- 运行 `python3 -m http.server 4173 --bind 127.0.0.1`：通过，静态 Web 示例可通过 `http://127.0.0.1:4173/` 访问。
- 使用 Chrome headless 截图：通过，截图保存到 `output/playwright/stock-chart-web.png`。
- 运行 `dart format lib/src/tencent_stock_chart_page.dart example/lib/main.dart example/test/widget_test.dart`：通过，未产生格式化变更。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，全部根目录测试通过。
- 在 `example/` 目录运行 `flutter test`：通过，默认空数据示例页测试通过。
- 重新运行 `flutter build web`：通过，生成默认空数据版本的 `example/build/web`。
- 使用 Chrome headless 重新截图：通过，默认打开已无行情线、成交量柱和买卖档数值。
- 运行 `dart format example/lib/main.dart example/test/widget_test.dart`：通过，股票代码输入栏相关文件格式正常。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，全部根目录测试通过。
- 在 `example/` 目录运行 `flutter test`：通过，示例页输入栏和默认空态测试通过。
- 重新运行 `flutter build web`：通过，生成带股票代码输入栏的 `example/build/web`。
- 使用 Chrome headless 重新截图：通过，页面顶部已显示股票代码输入框和加载按钮。
- 运行 `dart format example/lib/main.dart`：通过，URL symbol 初始化变更格式正常。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 在 `example/` 目录运行 `flutter test`：通过，示例页基础渲染测试通过。
- 重新运行 `flutter build web`：通过，生成支持 `?symbol=...` 的 `example/build/web`。
- 使用 Chrome headless 打开 `http://127.0.0.1:4173/?symbol=sz001896` 截图：通过，页面自动填入 `sz001896` 并显示行情图表。
- 运行 `dart format lib/src/tencent_stock_chart_painters.dart lib/src/tencent_stock_chart_page.dart`：通过，五日网格修复格式正常。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，全部根目录测试通过。
- 在 `example/` 目录运行 `flutter test`：通过，五日页签基础渲染测试通过。
- 重新运行 `flutter build web`：通过，五日网格修复已构建到 `example/build/web`。
- 运行 `dart format lib test example/lib example/test`：通过，版本发布前格式检查无变更。
- 运行 `flutter analyze`：通过，未发现分析问题。
- 运行根目录 `flutter test`：通过，全部根目录测试通过。
- 在 `example/` 目录运行 `flutter test`：通过，示例 Widget 测试通过。
- 运行 `flutter pub publish --dry-run`：第一次发现 `output/` 会进入发布包；补充 `.pubignore` 后再次运行，发布包内容正确，仅剩工作区未提交提醒。
- 发布前重新运行 `flutter pub publish --dry-run`：通过，发布包 0 警告。
- 运行 `flutter pub publish`：通过，pub.dev 返回 `stock_chart_flutter 0.1.2` 上传成功，页面最多可能需要 10 分钟生效。

## 待办专项

- 本轮已完成优化审查中列出的高优先级事项；后续按新增需求同步扩展 README、CHANGELOG 和测试覆盖。

## 备注

- 后续每次开发结束前都应检查并更新当天日志，确保完成事项和待办专项不遗漏。
